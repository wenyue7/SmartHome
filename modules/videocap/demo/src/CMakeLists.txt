SET(EXECUTABLE_OUTPUT_PATH ${PROJECT_BINARY_DIR}/bin)

AUX_SOURCE_DIRECTORY(. AVSTREAM_SOURCE)

ADD_EXECUTABLE(avstream ${AVSTREAM_SOURCE})

TARGET_LINK_LIBRARIES(avstream comm onvif ${LIBS} ${OpenCV_LIBS})
